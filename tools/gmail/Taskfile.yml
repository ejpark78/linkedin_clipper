version: '3'

tasks:
  sync:
    desc: Sync Gmail messages through the crawler Gmail tool.
    summary: |
      Sync Gmail messages by delegating to the crawler Gmail tool.

      [Usage Examples]
        - task gmail:sync
    cmds:
      - task crawler:gmail:sync
